import orderbookTipsHandler from '@handlers/orderbookTips.handler';

xdescribe('Orderbook Tips Handler', () => {
  test('should exists', () => {
    expect(orderbookTipsHandler).toBeTruthy();
  });
});
